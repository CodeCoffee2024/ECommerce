export const NotFound = (name: string): string => {
    return `${name} is not found`;
};
export const CreateSuccess = (module: string): string => {
    return `${module} successfully created`;
};
export const Failed = (module: string): string => {
    return `${module} failed to load`;
};

export const UpdateSuccess = (module: string): string => {
    return `${module} successfully updated`;
};
export const DeleteSuccess = (module: string): string => {
    return `${module} successfully deleted`;
};
export const EnableSuccess = (module: string): string => {
    return `${module} successfully enabled`;
};
export const DisableSuccess = (module: string): string => {
    return `${module} successfully disabled`;
};
